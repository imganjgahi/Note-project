import * as React from 'react';

type IProps = {}
const CreateNote = (props: IProps) => {
    
    return (
        <div>
            Create CreateNote
            
        </div>
    )
}

export default CreateNote